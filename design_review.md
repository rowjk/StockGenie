# Design Review - v1.6.0【審查結論，全數已採納實作】

1. **重啟防鎖死**：`/api/credentials` 完全獨立於 Shioaji 進程；金鑰填錯導致 daemon 崩潰時設定頁仍可修復。終止舊進程後等待 Port 8080 釋放再啟動。
2. **Demo 下單閉環**：下單虛擬攔截 + 模擬成交 + 動態更新假庫存/假餘額；賣出僅限假庫存且不超過持有量。
3. **資料一致性**：假資產趨勢終值精準等於假餘額+假持股市值。
4. **刪除保護與參數**：禁止刪除啟用中設定檔（須先切換）；重啟逾時 15s→30s；`.env` 合併寫入；文件範例金鑰一律佔位符。
5. **遮蔽固定長度**：Secret/密碼遮蔽不得洩漏原始長度。
6. **安全問答防護邊界聲明**：硬編碼 PIN 僅防辦公室肉眼窺視，不防本機抓包（F12 可見 payload）——本機桌面工具定位下為可接受折衷。
