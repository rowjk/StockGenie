# StockGenie - 股票儀表板設計規格書 (辦公室防窺版)

本設計規格書旨在為辦公室使用者量身打造一個**「低調、防窺、高質感」**的股票與資產監控儀表板。其設計核心是**偽裝成一般系統效能監控或雲端數據分析系統**（如 AWS CloudWatch、Jira、Datadog），徹底消除傳統看盤軟體顯眼的亮紅亮綠五顏六色特徵。

---

## 核心設計維度

### 1. 偽裝色彩系統 (Stealth Palette)

為了讓介面能自然融入辦公室日常開發與工作環境，系統提供了三種低調的色彩模式：

| 模式名稱 | 上漲顏色 | 下跌顏色 | 適用場景 |
| :--- | :--- | :--- | :--- |
| **科技藍調模式 (Slate Mode)** | 軟靛藍/亮天藍 (`#3b82f6` / `#60a5fa`) | 溫和暖灰 (`#94a3b8` / `#475569`) | **預設與推薦使用**。外觀看起來與一般的雲端流量、記憶體監控或系統效能圖表完全相同，極具科技感。 |
| **隱形黑白模式 (Stealth Mode)** | 無色彩標示 (使用預設文字色彩) | 無色彩標示 (使用預設文字色彩) | 旁人經常經過或座位毫無遮擋的辦公環境。漲跌僅依靠 `+` / `-` 或數值變化來識別，最不易被察覺。 |
| **柔和傳統模式 (Muted Color)** | 低飽和度莫蘭迪粉紅 (`#e11d48`) | 低飽和度莫蘭迪粉綠 (`#16a34a`) | 適合需要快速直覺反應漲跌，但又不想讓螢幕發出刺眼大紅色或亮綠色的工作場景。 |

---

## 介面佈局與功能區塊

### 1. 左側極簡導覽列 (Slim Sidebar)
* 採用寬度較窄的極簡設計 (64px)，僅顯示高質感的系統級圖示，滑鼠懸停時會提示中文文字，並使用圖示進行視覺偽裝：
  * **資訊總覽**：使用 📊（數據分析/磁帶卡片）圖示
  * **庫存部位**：使用 🗄️（伺服器機架/資料庫）圖示
  * **自選監控**：使用 📈（系統流量波形）圖示
  * **API 流量狀態**：使用 📡（網路訊號）圖示
  * **系統設定**：使用 ⚙️（齒輪設定）圖示

### 2. 上方狀態列 (Top Header)
* **帳號切換**：自動遮蔽敏感的身分證字號與完整帳號，僅顯示 `證券 (*889)` 或 `期貨 (*104)`，防止旁人偷窺。
* **伺服器狀態**：以一個精緻的綠色或灰色小圓點顯示 "伺服器已連線" 或 "伺服器已斷線"，讓外人誤以為是某個內部後端伺服器的狀態指示燈。
* **配色快速切換**：提供亮暗主題切換按鈕，以及配色下拉選單，可隨時在辦公桌前快速變更色彩模式。

### 3. 主內容區卡片 (Dashboard Cards)

#### 💳 資產概況卡片 (Asset Summary)
* **現貨可用餘額**：顯示當前銀行可用於證券交易的交割餘額。
* **交易額度監控**：以精緻的進度條顯示「已用額度 / 總交易額度」，外觀與硬碟容量或記憶體使用率極度相似，不易引人注意。
* **期貨保證金 (動態顯示)**：若存在期貨帳戶，會顯示保證金餘額、未平倉損益及風險指標，並始終維持低調的色系。

#### 📈 總資產歷史趨勢圖 (Asset History Trend)
* **功能描述**：滾動記錄過去 3 個月 (90 天) 的每日總資產收盤價值趨勢。
* **數據計算**：`總資產 = 現貨餘額 + 股票持股市值 + 期貨保證金餘額`。
* **自動記錄機制**：當網頁在交易日運行時，會自動將計算後的「當日資產總額」發送至後端。後端會寫入並更新 `asset_history.json` 檔案。每日僅保留最後一筆（收盤）的記錄。
* **手動補錄編輯器**：在設定面板中，使用者可以隨時透過簡易的資料表單手動新增、修改或刪除過去 90 天的每日資產收盤數據，使走勢圖保持連貫完整（初始值皆歸 0）。

#### 🗂️ 庫存部位列表 (Positions Table)
* 乾淨簡潔的無邊框表格。顯示「股票代號」、「股票名稱」、「交易方向」、「持有數量 (股)」、「買進均價」、「目前現價」、「未實現損益」與「損益率」。
* 點擊任何一檔股票代號即可直接彈出右側交易面板。

#### 👁️ 自選監控與微型即時走勢圖 (Watchlist & Sparklines)
* 支援 localStorage 儲存常用監控代號。
* 每個監控項目的中間會自動渲染一個微型走勢圖 (Sparkline)：
  * 使用 HTML5 `<canvas>` 動態繪製。
  * **無網格、無坐標軸、無數字刻度**，單純呈現當日成交價的起伏波動。
  * 呈現效果宛如伺服器叢集的 CPU 或網絡負載波動，具備極佳的隱蔽性。

#### 🛠️ 交易委託安全抽屜 (Hidden Trading Drawer)
為了防止使用者在辦公室因分心或緊張造成交易誤判，交易系統採取了雙重防護：
* **安全鎖保護**：點擊股票開啟抽屜時，下單表單上方會有一個「安全鎖定狀態」覆蓋層，必須手動點擊「解除安全鎖」按鈕才能啟用輸入與送出。
* **全繁體中文介面**：所有下單欄位與選項（股票代號、交易動作：買進/賣出、交易單位：整張/零股、交易條件：現股/融資/融券、價格類型：限價/市價、委託價格與委託數量）均採用純中文顯示，無英文代稱，保障交易安全。
* **明確的交易單位區分**：提供「整張（以張為單位，1張=1000股）」與「零股（以股為單位，1~999股）」下拉選單，無論是買入或賣出，皆須明確指定交易單位以防止下錯單。
* **最終自訂確認彈出視窗 (Modal Pop-up)**：送出委託前，系統會彈出一個與 Slate/Stealth 科技感主題高度融合的暗色磨砂玻璃交易確認對話框，將股票代號、交易動作（買進以藍色標示、賣出以暖灰標示）、交易單位（明標整張/零股單位）、交易條件、價格類型、委託價格與數量等明細以大型字體條列。使用者點選「確認送出」後才正式發送，以杜絕誤觸或防窺疏漏。

---

## 技術與效能架構

1. **極簡原生技術棧 (Vanilla Stack)**：
   - 前端完全使用原生 HTML5、CSS Variables、JavaScript ES6 與 Canvas。無任何第三方庫依賴，加載速度極快，且不留任何多餘腳本，適合本機私密運行。
2. **反向代理與同源安全通訊 (Proxy Bypass)**：
   - 瀏覽器出於安全性考量，不允許跨 Port 發送帶有 JSON 格式的跨域 POST 請求（會發送 OPTIONS 預檢並被拒絕）。為此，後端在 Port 8081 上實作了 `/proxy/` 反向代理路由，由 Python 內建的 **多執行緒伺機器 (ThreadingHTTPServer)** 將請求轉發給 Port 8080 的 Shioaji API 本地伺服器。此舉完全達成了「同源請求」，100% 繞過瀏覽器的 CORS 安全限制，保證交易穩定。
3. **流量防護與 API 頻率限制 (Rate Limiting)**：
   - 配合 Shioaji 行情 Snapshot 與 Tick/Kbar 查詢的次數限制，前端只在加入自選監控或點選商品時進行 Tick 行情查詢，並將所有自選股的快照查詢合併為單次請求。此外，餘額、庫存與交割款設有 15 秒（可於設定中調整）的定時輪詢，防止觸發 API 限流。
4. **長連線 (SSE) 直連優化**：
   - 即時成交與委託事件回報的 EventSource 保持直連 `http://127.0.0.1:8080`。這樣可以使串流數據直接傳遞，不佔用並阻塞 Python 8081 的後端執行緒。
