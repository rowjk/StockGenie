# StockGenie - 實作與使用指南

我們已成功實作並驗證了專為辦公室環境設計的永豐證券股票交易與資產監控儀表板。所有的界面、主控台日誌、錯誤提示及文檔已全面翻譯為繁體中文，以防範任何交易上的誤會。

---

## 🛠️ 已實作的變更與檔案

系統由輕量級的 Python 後端伺服器與單頁式響應式網頁前端所組成：

### 1. 後端伺服器與設定
* [dashboard.py](./dashboard.py)：自訂 Python 後端。負責在背景啟動 Shioaji 本地伺服器（Port 8080），並在 Port 8081 提供前端靜態檔案服務。同時提供 `/api/asset-history` 路由，支援每日總資產歷史紀錄的儲存、讀取與過期清理。所有的終端機 Print 輸出已全面轉為繁體中文。在此版本中，我們已**優化為多執行緒伺服器 (ThreadingHTTPServer)**，確保前端大量定時輪詢併發請求不會發生阻塞或卡死，並**自訂 UTF-8 的 JSON 錯誤響應機制**，徹底解決因 Windows 斷線等錯誤訊息包含中文而觸發 Python latin-1 編碼崩潰的問題。此外，也優化了 `CA_CERT_PATH` 憑證路徑安全解析邏輯以同時完美支援絕對路徑與相對路徑。**v1.3.0 新增**：啟動等待機制改為主動輪詢 `http://127.0.0.1:8080/api/v1/auth/usage`（最多 30 秒），取代固定 `sleep(8)` 以提升穩定性；每次寫入 `asset_history.json` 前自動備份為 `asset_history.bak.json`，防止資料損毀遺失。**v1.5.0 新增**：`/api/us-chart` 美股行情代理路由，轉發 Yahoo Finance 公開 chart API（代碼格式驗證、固定查詢組合白名單、盤中快取 60 秒/日線快取 30 分鐘、gzip 傳輸、查無代碼回 404）。**v1.5.2 新增**：修正 TWSE 憑證 SSL Lock 宣告未定義的 NameError 隱患；優化歷史資產自動清理邏輯，避免過度清理導致手動導入的長歷史數據被刪除。
* [asset_history.json](./asset_history.json)：本地 JSON 歷史資產數據庫（初始為空 `{}`）。
* [啟動儀表板.bat](./啟動儀表板.bat)：Windows 批次啟動腳本，支援雙擊一鍵運行，已中文化所有啟動與環境錯誤提示。

### 2. 前端網頁介面與邏輯
* [web/index.html](./web/index.html)：網頁主結構，採用低調的系統監控風格排版。下單抽屜與 API 狀態視窗中已清除所有英文括號，實現 100% 繁體中文。**v1.3.0 新增**：移除開發期 remote-log 攔截器（原本每次 console.log 都會打 POST 回 8081），減少不必要的網路請求。
* [web/style.css](./web/style.css)：Vanilla CSS 樣式表。定義了亮/暗主題，以及四種防窺配色模式：
  * **科技藍調 (Slate Mode)**（預設）：溫和的靛藍與暖灰，外觀極像 AWS 或 Jira 流量監控。
  * **隱形黑白 (Stealth Mode)**：完全不使用任何紅綠或藍色色彩，最適合防窺。
  * **柔和傳統 (Muted Color)**：低對比度的柔和粉紅與粉綠，保留基本漲跌視覺反饋。
  * **駭客任務 Matrix (v1.4.2)**：純黑底螢光綠終端機風格，僅支援暗色背景（主題切換按鈕自動鎖定）。
* [web/app.js](./web/app.js)：前端應用邏輯。處理帳號載入、定時輪詢餘額與庫存、localStorage 自選監控與 Canvas 微型走勢圖繪製。此外，亦處理即時 SSE 委託成交日誌提示（Toast）以及 100% 繁體中文的交易單安全確認視窗。同時，已在下單（`place_order`）的 payload 結構中補上 `person_id`（身分證字號）欄位，徹底解決因缺少該參數可能導致 Shioaji API 伺服器回傳 500 交易失敗的問題。**v1.3.0 新增**：下單 payload 的 `exchange` 欄位改從持倉資料動態帶入（支援上市 TSE / 上櫃 OTC 股票），取代原本寫死 `'TSE'` 的錯誤邏輯；每日資產紀錄改為當日首次成功寫入後即停止，避免每 15 秒重複覆寫 JSON 浪費 I/O；修正零股持倉市值計算：新增 `isOddLot()` / `lotMultiplier()` helper，根據 `order_lot` 欄位（`IntradayOdd` / `Odd` / `BulkOdd`）判斷數量單位，零股不再錯誤乘以 1000；持倉表格數量欄位同步顯示單位（`張` / `股`）。

---

## 🚀 如何啟動儀表板

1. **一鍵啟動**：在檔案總管中雙擊 **[啟動儀表板.bat](./啟動儀表板.bat)**（或在終端機執行 `python dashboard.py`）。
2. **自動開啟**：腳本會啟動背景 API 與網頁伺服器，偵測到 Shioaji API 就緒後自動在預設瀏覽器開啟 **`http://127.0.0.1:8081`**（通常在 10 秒內，最多等待 30 秒）。
3. **主控台監控**：CMD 控制台視窗此時將會以全中文顯示運行狀態。如需結束服務，只需在控制台按下 **Ctrl + C** 即可安全終止所有背景程序。

---

## 💡 核心功能操作說明

### 1. 防窺配色與響應式排版切換 (v1.3.18 優化)
* **防窺配色**：您可以點擊網頁右上角的下拉選單，在「科技藍調」、「隱形黑白」及「柔和傳統」三種配色方案間自由切換。點擊旁邊的太陽/月亮圖示可以切換亮色或暗色主題。
* **響應式天區收縮**：當您縮小瀏覽器視窗時，頂部天區（Header）會自動收縮（隱藏系統標題與連線狀態文字，僅保留運作燈號與倒數時鐘），如同左側功能欄的收起效果，以防範任何窄螢幕下的排版歪斜與折行。

### 2. 每日收盤財產紀錄 (滾動保留，上限 3000 筆)
* 系統在您開啟網頁且 API 連線成功時，會自動計算您的「總資產 = 現貨餘額 + 股票持股市值」（v1.4.0 起期貨功能已退場），並記錄為當日的收盤值。自 v1.5.2 起，只有在總歷史紀錄大於 3000 筆時才啟動清理，且清理方式改為依日期排序保留最新 3000 筆，避免破壞手動導入的長期歷史。
* 若要手動補齊過去的財產歷史記錄，請點擊左側導覽列的 **「系統設定」**，即可在「資產歷史數據編輯器」中輸入日期與總金額進行儲存、修改或刪除，數據會同步更新至首頁的折線圖上。

### 3. 安全交易委託 (雙重防護)
* 點擊「庫存部位」或「自選監控」列表中的股票代號，右側會平滑滑出交易抽屜。
* **第一重解鎖**：交易面板預設會處於「安全鎖定狀態」，必須手動點擊「解除安全鎖」按鈕才能啟用輸入與下單。
* **整張與零股區分**：在「交易單位」下拉選單中，您可以明確選擇：
  * **整張**：委託單位為張（1張 = 1000股）。
  * **零股**：委託單位為股（1 ~ 999股）。
* **第二重自訂彈出視窗確認**：填寫完畢並點擊「確認送出委託」後，網頁會彈出專屬科技暗色風格的「交易委託送出確認」彈出視窗 (Modal Pop-up)，展示股票代號、買賣方向、委託價格、數量與交易條件等重要細節。請您務必仔細核對無誤後，點選「確認送出」，訂單才會真正透過 Shioaji API 代理送往永豐證券交易所。

### 4. 唯讀金鑰安全檢核與攔截 (v1.3.17 新增)
* **下單攔截阻斷**：當您使用唯讀或限制了交易權限的金鑰啟動服務時，點選任何下單或委託按鈕（如持倉、自選股中的「下單」按鈕，或右上角「快速下單」輸入框），系統將會立即彈出瀏覽器告警提示 **「下單權限關閉」**，並拒絕開啟交易面板。
* **後端防禦攔截**：本機 python 代理伺服器亦會於 `/order/place_order` API 層進行雙重防護檢核，直接拒絕任何來自外部腳本或繞過前端發送的下單 POST 請求，保障您的帳戶與資產絕對安全。

### 5. 大盤指數監控 (v1.3.19 新增 / v1.3.20-v1.3.23 優化)
* **大盤指數代碼**：本系統支援永豐 Shioaji 大盤加權指數的即時監控。使用者只需在右上角自選股搜尋框中，輸入大盤加權指數代碼 **`TSE001`**（或直接輸入 **`001`**），即可新增至自選清單。
* **交易防護限制**：大盤加權指數屬於非交易性商品，加入自選清單後，系統會自動辨識其 `security_type` 為 `"IND"` 並隱藏「下單」按鈕，防止點擊產生無效委託錯誤。
* **專屬底色與點數顯示 (v1.3.20-v1.3.23 優化)**：指數商品卡片會自動以淡灰底色（`--bg-tertiary`）呈現，以便與普通股票進行視覺區隔。同時，漲跌幅徽章會同步顯示「即時漲跌點數 (漲跌百分比)」（例如 `+150.25 (+0.82%)`），且寬度擴展至 `140px` 確保不折行。在 **v1.3.22** 中，我們在卡片最右側加入了透明的 `42px` 佔位區塊，解決了大盤指數因無「下單」按鈕而導致價格區塊歪向右側、無法與普通股票對齊的問題。在 **v1.3.23** 中，進一步修正了隱形黑白模式下大盤徽章背景的色彩對比度，將指數徽章背景強制指定為相對深色的 `--bg-secondary`，以防範底色重疊融合造成字體背景消失。
* **分時圖與 MA 指標**：大盤指數加入後，會完整呈現實時分時圖 (Tick Chart)、歷史日 K 線圖與移動平均線 (MA) 分析。

### 6. 自選清單自訂排序 (v1.3.20 新增)
* **排序按鈕 (▲ / ▼)**：滑鼠游標移至自選股卡片上時，最左側會浮現低調的微型上移（▲）與下移（▼）箭頭按鈕。
* **即時交換與持久化**：點擊箭頭按鈕可以即時在清單中上下互換項目順序，並且順序會自動寫入 `localStorage`，下次重啟網頁時依然能保留您自訂的看盤排序。排序操作經過防冒泡處理，點擊時不會誤觸右側詳細圖表的選取狀態。

### 7. 自選股響應式置中排版與重繪 (v1.3.21 新增)
* **優雅置中排版**：自選卡片中的微型走勢圖（Sparkline）改為 `flex: 1` 彈性拉伸，並限制最大寬度為 `160px`，配合 `margin-left: auto` 將價格與按鈕推至最右側。當您拉寬或展開儀表板時，走勢圖能優雅置中，保持左右間隔均稱美觀，徹底擺脫擠在最右邊以及大片空白的視覺缺點。
* **視窗防拉伸重繪**：系統內置了視窗縮放偵聽器，在您拖動或改變視窗大小時會自動重繪微型走勢圖，確保折線圖永遠呈現最精確的比例，不發生橫向拉伸或變形。

### 8. 自訂趨勢圖顯示與隱形黑白按鈕對比度優化 (v1.3.25 優化)
* **「顯示趨勢圖」Toggle 開關**：在自選監控標題右側新增開關，**預設為關閉**。不顯示走勢圖時，清單卡片將隱藏 Canvas 元件，完全免除微型走勢的繪製，讓看盤介面極致防窺與簡化，並有效省電、省流量及減少瀏覽器負載。狀態會自動持久化至 `localStorage`。此功能標題已根據使用者反饋修訂為「顯示趨勢圖」。
* **黑白按鈕與 Tab 懸停對比度修正**：修正了「隱形黑白模式」下按鈕（如新增監控、代號下單按鈕及分時/均線切換 Tab）因背景與字體顏色同為白色而導致看不見字的問題。系統會將按鈕及 Tab 懸停 (hover) 文字顏色重設為與背景強烈對比的 `var(--bg-primary)`，確保在所有狀態下字體均清晰可見。
* **徹底停用靜態快取**：為了解決瀏覽器快取舊版網頁、CSS 樣式或 JS 檔案所引發的版面顯示異常，後端 Python 伺服器 `DashboardHandler` 已實作防快取標頭（Cache-Control: no-store, no-cache 等），確保每次重新整理網頁時皆能百分之百載入最新變更。

### 9. 期貨退場與新總覽卡片 (v1.4.0 新增)
* **期貨功能全面退場**：移除期貨保證金卡片、期貨部位表格與相關 API 輪詢，系統專注於證券帳戶。
* **新卡片**：總覽新增「庫存證券總市值」與「總資產（現金餘額 + 證券市值）」兩張卡片，隨輪詢即時更新。

### 10. 一鍵隱私遮蔽 Boss Key (v1.4.0 新增)
* **觸發方式**：點擊 Header 的「眼睛斜線」圖示，或按下鍵盤 **`Esc`** 鍵。
* **遮蔽範圍**：所有資產金額（餘額、額度、市值、總資產、交割款、持倉表、歷史表）以 `*****` 遮蔽；**所有 Canvas 圖表**（資產趨勢、已實現損益、分時、均線、sparkline）即刻清空並印上 `[DATA MASKED]`，防止透過折線趨勢或 Y 軸座標反推資產。遮蔽期間輪詢更新也不會洩漏數字。
* **解除**：再按一次 `Esc` 或再點一次圖示，所有圖表自動重繪。

### 11. 終端機日誌看盤模式 Terminal Log Mode (v1.4.0 新增)
* **觸發方式**：點擊 Header 終端機圖示，或在 400ms 內**快速雙擊空白鍵**（系統已主動 `preventDefault()`，雙擊不會造成網頁向下滾動跳動；輸入框內打字不受影響）。
* **偽裝畫面**：全螢幕黑底綠字伺服器日誌。自選股即時價格偽裝為 `[INFO] Heartbeat check code 2330: price=585.00`，持倉損益偽裝為 worker 記憶體訊息，並穿插擬真系統雜訊。關閉時自動清空所有日誌行，不留行情殘跡。
* **快速退出**：終端機模式開啟時按 `Esc` 會優先關閉終端機。

### 12. 資訊總覽卡片自訂顯示 (v1.4.0 新增)
* 於「系統設定」面板勾選欲顯示的卡片（現貨餘額、交易額度、持股市值、總資產、資產趨勢、交割款、已實現損益、重大訊息、除權息行事曆），即時生效並持久化於 `localStorage`，重新整理後保持。

### 13. T+2 違約交割缺口主動警示 (v1.4.0 新增)
* 系統依交割款逐日累計計算 T+2 預估餘額，若低於 0，總覽最上方會顯示低調橘黃色警戒條與資金缺口金額，提醒於交割日前補足款項。

### 14. 盤中委買委賣力道分析 (v1.4.0 新增)
* 個股明細面板新增「多空力道對比條」，依快照之委買/委賣掛單張數即時顯示買方力道百分比。
* 注意：Shioaji HTTP snapshot 提供的是**最佳一檔**委買/委賣量（非五檔加總），力道比例以此計算。

### 15. 已實現損益月度圖表 (v1.4.0 新增 / v1.4.2 優化)
* 總覽新增「已實現月度損益」條形圖，調用 Shioaji 本地 `/portfolio/profit_loss` 帳務 API。前端未帶時間參數時，後端 Proxy 自動補上**前 365 天**查詢區間。正負月份以漲跌色區分，標題顯示近一年合計。
* **v1.4.2**：每根 bar 上方（負值在下方）直接標示該月損益金額；主題與配色切換時會即時以新色彩重繪。
* 名詞說明：「已實現損益」是**實際賣出後**的損益（賣出價 − 成本，含費用），與庫存市值的帳面浮動（未實現損益）不同。

### 16. TWSE 重大訊息與除權息行事曆 (v1.4.0 新增)
* **重大訊息日誌**：後端 `/api/twse-announcements` 介接 TWSE OpenAPI（每日重大訊息），以系統事件 Log 形式流動顯示自選股公告，每 10 分鐘自動更新（後端快取 10 分鐘，避免高頻請求 TWSE）。
* **除權息行事曆**：後端 `/api/twse-dividends` 取得 TWSE 除權息預告，顯示自選股「即期除權息日期」「除權/除息類別」與「預估股利」，利於存股規劃。民國日期已自動轉為西元。
* 需要對外網路連線；TWSE 來源異常時看板顯示空狀態，不影響其他功能。
* **v1.4.1 憑證鏈處理**：TWSE 的 HTTPS 憑證鏈缺少 Subject Key Identifier，新版 Python 會驗證失敗。系統會自動降級為寬鬆 SSL 模式並記住狀態（終端機印黃色提示），僅伺服器啟動後的第一次抓取較慢。此 API 僅讀取公開市場公告，無帳務資料風險。

### 17. 歷史數據匯出 / 匯入與安全校驗 (v1.4.0 新增)
* **匯出**：「系統設定」→「匯出 JSON」下載 `asset_history_YYYY-MM-DD.json` 備份。
* **匯入**：「匯入 JSON」選擇備份檔上傳。後端進行**嚴格校驗**（必須為 `[{date, value}]` 陣列、日期 `YYYY-MM-DD`、數值為非負有限數、不得含未知欄位、上限 5000 筆 / 1MB），**整批通過才合併寫入**，任何一筆錯誤即整批拒絕並回報原因，舊有歷史資料不會被破壞（寫入前自動備份 `.bak`）。

### 18. 自選股 20 檔上限與分批快照 (v1.4.0 新增)
* 自選監控清單上限 **20 檔**；快照查詢以**每 10 檔為一組**分批傳送，降低 API 限流超時風險。

### 19. 帳務並行查詢與首屏加速 (v1.4.1 新增)
* 餘額、交易額度、庫存、交割款四支帳務 API 改為**並行**發出（`Promise.allSettled`），行情快照獨立不被帳務卡住；自選清單初始化完成後立即補打一次快照，開頁約 1–2 秒即有報價。
* 永豐帳務後台偶發單支查詢緩慢（甚至 60 秒逾時）時，僅該卡片顯示 `--`，其他功能不受影響；後端 Proxy 對帳務端點放寬等待至 30 秒，並將所有上游錯誤以黃色 `⚠` 印至終端機便於排障。
* 若懷疑永豐後台異常，先關閉儀表板後執行 `python sdk_probe.py`，可直接區分是本機程式還是券商後台的問題。

### 20. 駭客任務 The Matrix 顯示風格 (v1.4.2 新增)
* 配色選單新增「駭客任務 Matrix 風格」：純黑背景、螢光綠文字、金額數值輝光，所有 Canvas 圖表（資產趨勢、損益、分時、Sparkline）同步綠化。
* **僅支援暗色背景**：選用時系統強制切換為暗色主題，並**鎖定**亮/暗主題切換按鈕（滑鼠停留會顯示「Matrix 風格僅支援暗色背景（鎖定）」）；切回其他風格時按鈕解鎖並自動還原您原本儲存的主題偏好。
* 搭配雙擊空白鍵的 Terminal Log Mode（黑底綠字日誌）使用，整體視覺完全一致。

### 21. 美股自選監控 (v1.5.0 新增)
* **進入方式**：點選側邊欄的地球圖示「美股指數」分頁。本功能免 API 金鑰，行情由後端 `/api/us-chart` 路由代理 Yahoo Finance 公開資料（**延遲約 15 分鐘**，狀態列有明確標註與最後更新時間）。
* **新增自選**：在上方搜尋框輸入任意美股代碼後按「新增監控」或 Enter。支援指數（`^GSPC`、`^DJI`、`^IXIC`）、個股（`AAPL`、`BRK-B`）與 ETF（`VOO`、`QQQ`），小寫自動轉大寫，商品名稱自動帶入（如 Apple Inc.）。查無代碼時狀態列會顯示紅字提示。
* **移除與排序**：點選商品開啟右側明細卡後，按右上角垃圾桶即可移除；清單卡片左側 ▲▼ 可上下調整順序。清單存於瀏覽器 localStorage（`usWatchlist`），首次使用預設帶入 `^GSPC`（S&P 500 指數）與 `VOO`，上限 20 檔。
* **明細面板**：比照台股自選，提供昨收、開高低收、成交量（指數多無此值，顯示 `--`）、MA5/20/60/240 數值格，以及「分時走勢」（當日 5 分 K）與「均線走勢」（2 年日線取近 1 年，含四條 MA）雙 tab 圖表；Boss Key 遮蔽與 Matrix / Stealth 配色完全適用。
* **樣式區分**：指數商品依 Yahoo `instrumentType` 自動套用淡灰底專屬樣式（與台股 `TSE001` 一致）；個股與 ETF 為一般卡片。所有商品漲跌徽章統一為「漲跌點數 (漲跌百分比)」雙顯示（v1.5.0 起台股個股亦同步此格式）。
* **流量節約**：行情每 60 秒更新一次，僅在本分頁開啟且網頁位於前景時輪詢；後端另有盤中 60 秒 / 日線 30 分鐘快取，避免高頻打 Yahoo API。**v1.5.1 起**，停留在本分頁期間台股自選快照輪詢同步暫停（帳務查詢與 T+2 警示照常），切回其他分頁時立即補抓一次最新快照。
* **美股休市時**：分時圖顯示前一交易日的完整走勢，價格為前一交易日收盤價。

