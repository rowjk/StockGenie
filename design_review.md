# 第三方架構與安全審查報告 (Design Review) - StockGenie v1.6.0

本審查報告由獨立系統架構師與安全性稽核員（第三者角度）針對 `prd.md`、`sa.md` 與 `implementation_plan.md` 進行全面評估。評估重點在於**安全性防護**、**子進程生命週期強健性**、**Demo 模式防洩漏機制**，以及**防禦性編程邊界**。

---

## 1. 架構完整性與強健性評估 (Architectural Robustness)

### 1.1 子進程重啟與線程安全 (Thread Safety during Daemon Restart)
- **現狀審查**：SA 文件設計了 `shioaji_proc_lock` 鎖來保護 `subprocess.Popen`，防止多線程併發重啟。
- **風險分析**：在 `shioaji_proc.terminate()` 之後直接進行 `Popen` 啟動新進程，若 Windows 系統回收 Port 8080 速度較慢，可能導致新進程因為 `Port 8080 already in use` 而啟動失敗。
- **改進建議**：
  - 在關閉舊進程後，應加入短暫的 socket 端口釋放檢查，或在重啟失敗時重試。
  - 在子進程終止後，確認 `shioaji_proc` 確實結束（如利用迴圈等待最多 3~5 秒，確認 Port 釋放）再啟動新進程。

### 1.2 重啟失敗的降級與容錯 (Fallback on Start Failure)
- **現狀審查**：前端設有 15 秒重啟超時。
- **風險分析**：若使用者填入無效的金鑰，`shioaji.exe` 在重啟後登入可能直接崩潰退出，導致後端完全無法提供 Proxy 服務，此時前端將無限顯示離線，使用者無法再點選「系統設定」來改回舊金鑰，系統將陷入死鎖。
- **改進建議**：
  - **重要**：不論 Shioaji daemon 是否在線，**後端本地的 `/api/credentials` 及變更設定 API 必須完全獨立運作**（不能依賴 Shioaji 進程），且前端的「系統設定」頁面載入不可被 `/proxy/` 的斷線所阻斷。
  - 即使 Shioaji 伺服器離線，前端依然要允許使用者進入系統設定修改金鑰，否則一旦填錯一次金鑰，軟體將永久鎖死（必須手動改 `.env`）。

---

## 2. 安全性與隱私防護評估 (Security & Privacy Audit)

### 2.1 金鑰遮蔽與更新防禦 (Masking & Edit Defensive Handling)
- **現狀審查**：後端傳送遮蔽金鑰，且在儲存時偵測遮蔽符（如 `...`）以決定是否覆寫。
- **安全性評估**：此項設計非常優秀，完美解決了前端金鑰外洩問題。
- **細節漏洞**：CA 憑證密碼（`ca_password`）與 `secret_key` 在遮蔽時若顯示為相同長度的 `●●●●●●●●`，是否會被暴力破解？
- **防禦加強**：遮蔽字串應統一返回固定長度（例如恆為 `●●●●●●●●` 或 `********`），切勿透漏真實金鑰或密碼的長度。

### 2.2 二次安全驗證碼 (Verification Code Security)
- **現狀審查**：敏感 API 限制帶入 `verification_code === "PEA6"`。
- **安全性評估**：
  - 這是一種「隱藏式 PIN 碼」的防窺設計，在辦公室防窺情境下很實用（同事不知道 PEA6 代表什麼，且選項是洗牌的）。
  - 但請注意：由於前端與後端的驗證碼是**硬編碼 (Hardcoded)** 的常數 `"PEA6"`，若有技術背景的旁觀者查看前端網路傳輸（F12 Network），會直接在 POST payload 中看到 `"verification_code": "PEA6"`，這會使該防護在面對技術型攻擊時失效。
  - **架構師建議**：這套系統定位為「本機桌面工具 (Local Desktop App)」，防範對象主要是「辦公室過路人的肉眼窥視」，因此硬編碼 PIN 是可接受的折衷方案。但應在 SA 中明確標註此防護的邊界（僅限防物理窺視，不防本機抓包）。

---

## 3. Demo 模式擬真度與防漏審查 (Demo Mode Assessment)

### 3.1 仿真度細節缺失 (Lack of Granular Mocking)
- **現狀審查**：Demo 模式回傳了行情波動、餘額與歷史趨勢。
- **細節漏洞**：
  1. 當使用者在 Demo 模式下點擊「快速下單」或在庫存部位點擊「下單交易」時，會發生什麼事？
  2. 若無處理，前端會彈出實體的交易確認 Modal，並在送出時發送 POST 請求至 `/proxy/api/v1/order/place_order`，這將導致下單失敗或向雲端發送無效交易。
- **改進建議**：
  - Demo 模式下，下單操作必須被「虛擬攔截」：送出下單時，直接彈出虛擬的「[DEMO 模式] 委託已送出」Toast 提示，並隨機在 1~2 秒後提示「[DEMO 模式] 委託已成交（價格 $XXX，張數 X 張）」，並**動態將成交股數加進 Mock 庫存持倉中**，扣除假可用餘額！
  - 這會讓整個 Demo 流程**驚艷無比 (Wow the user)**，演示者可以當場表演下單、成交、庫存增加、餘額減少的完整閉環，這將使此軟體極具專業感！

### 3.2 數據一致性 (Data Consistency)
- **風險分析**：若資產歷史趨勢（90天）的最後一個數值與目前的「可用餘額 + 庫存市值」不相等，對細心的觀眾來說會顯得虛假。
- **改進建議**：
  - 在生成假數據時，應動態使「假資產趨勢折線圖」的最後一天數值，精準等於目前的「假餘額 + 假庫存市值」總和，讓圖表與總覽數值完美對齊，達到 100% 的真實感。

---

## 4. 文件 Review 結論與改進清單

為確保 v1.6.0 的交付品質達到企業級水準，我們將以下三項優化直接納入實作計畫：

1. `[優化 1]` **重啟防鎖死安全機制**：即使 Shioaji server 重啟失敗，網頁的「系統設定」頁面與讀寫/修改金鑰 API 必須能正常響應，允許使用者修復金鑰。
2. `[優化 2]` **Demo 模式「下單/成交」閉環模擬**：在 Demo 模式下，使用者下單會被模擬成交，並動態更新假餘額、假庫存（部位增加/扣除資金），使演示效果極致逼真。賣出操作僅限既有假庫存且不得超過持有股數。
3. `[優化 3]` **資料一致性校準**：假資產趨勢圖的終端點數值，必須與假餘額 + 假持股市值精準一致。
4. `[優化 4]` **刪除保護與參數調整**（複審追加）：禁止刪除啟用中設定檔（須先切換）；重啟逾時由 15 秒放寬至 30 秒；`.env` 採合併寫入以保留非金鑰設定；SA 文件範例金鑰一律改為佔位符，嚴禁將真實金鑰寫入任何文件。
