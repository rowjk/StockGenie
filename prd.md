# PRD - API 金鑰多組管理與 Demo 模式 (v1.6.0)【已完工驗收】

## 功能一：API 金鑰多組管理
* `credentials.json`（專案根目錄，被 `*.json` gitignore）存 `active_index` + `profiles[]`（name/api_key/secret_key/ca_cert_path/ca_password）。不存在時自 `.env` 自動初始化第一組（無縫升級）。
* 後端回傳一律遮蔽：API Key 前 6 後 4（`ABCDEF...WXYZ`）；Secret/CA 密碼固定長度 `●●●●●●●●`（不洩漏原長）。
* 編輯表單：名稱必填；金鑰欄留空或維持遮蔽格式（含 `...`/`●`/`*`）＝不更新該欄。已知限制：真實金鑰本身含遮蔽符時無法經編輯更新，只能刪除重建。
* 所有變更（儲存/切換/刪除）：前端彈安全問答 + 後端驗證 `verification_code 與後端預設碼一致`，錯誤回 403。
* 切換或儲存啟用中設定時：金鑰合併寫入 `.env`（供 monitor.py）→ 終止舊 `shioaji.exe` → 等 Port 8080 釋放 → 新環境變數重啟。前端鎖定遮罩每秒輪詢，30 秒逾時顯示錯誤並提供「強制解鎖返回」（不可永久卡死）。
* 保護：不可刪除最後一組（400）；不可刪除啟用中設定檔，須先切換（400，前後端雙重校驗）。

## 功能二：Demo 演示模式
* 設定頁開關，持久化 `localStorage('demoMode')`。開啟：Header 顯示 DEMO 徽章、前端攔截所有 `/proxy/` 與本機帳務端點、零實體請求、可完全離線。關閉（reveal 真實資產）：須通過 預設驗證碼 安全問答。
* 假數據規格（初始值，其後隨輪詢隨機漫步 ±0.1~0.3% 波動）：
  - 餘額 1,250,300；額度 5,000,000 / 已用 850,000 / 可用 4,150,000。
  - 持倉：2330 台積電 2000股 均910 現950（+80,000/+4.40%）；2317 鴻海 5000股 均185 現180（-25,000/-2.70%）；2454 聯發科 1000股 均1280 現1310（+30,000/+2.34%）；2603 長榮 3000股 均190 現198.5（+25,500/+4.47%）。
  - 交割款 T0 +12,500 / T1 -25,000 / T2 +80,000；月度損益近 12 月合理起伏；假公告與除權息。
  - 90 天資產趨勢震盪向上帶回檔，**終值動態校準＝假餘額＋假庫存市值**。
  - 點股票可載入仿真分時圖與 MA 日線圖；美股指數同步波動仿真。
* 下單閉環模擬：虛擬攔截不發實體請求。買進：模擬成交後加假庫存、扣假餘額；賣出：僅限既有假庫存且不得超過持有量（超出提示錯誤），成交後減庫存增餘額。
* 邊界：重啟逾時 30 秒（Shioaji 登入+CA 驗證可能 >10 秒）；離線環境可直接進設定開 Demo 預覽。
