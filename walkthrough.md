# StockGenie - 操作手冊（精簡版）

## 啟動與停止
雙擊 `啟動儀表板.bat`（或 `python dashboard.py`）→ 自動開 `http://127.0.0.1:8081`；控制台 `Ctrl+C` 安全停止。

## 功能操作

* **防窺配色**：右上下拉切換 Slate（預設）/ Stealth（無色差）/ Muted / Matrix（強制暗色）；太陽月亮鈕切換亮暗。
* **Boss Key**：`Esc` 或眼睛鈕。金額變 `*****`、所有 Canvas 清空印 `[DATA MASKED]`，輪詢中也不洩漏。
* **Terminal Log Mode**：雙擊空白鍵（400ms 內）或終端機鈕。全螢幕偽裝伺服器日誌，行情化為 heartbeat 訊息，關閉時清空殘跡；`Esc` 優先退出此模式。
* **系統設定**：點側邊欄齒輪 → 安全問答（6 個洗牌選項，正解 `PEA6`）。內含：配色、刷新頻率、卡片顯示開關、資產歷史編輯器/匯出/匯入、API 憑證管理、Demo 模式開關。
* **下單（雙重防護）**：點庫存或自選代號開抽屜 → 點「解除安全鎖」→ 填單（整張/零股、現股/融資/融券、限價/市價）→ 確認 Modal 核對後送出。唯讀金鑰或 `TRADING_ENABLED=false` 時前後端皆攔截。
* **自選監控**：上限 20 檔，▲▼ 排序，localStorage 持久化；大盤輸入 `TSE001` 或 `001`（IND 無下單鈕）；「顯示趨勢圖」開關預設關。明細含開高低收、MA5/20/60/240、委買賣力道條（最佳一檔）、分時/均線雙 tab。
* **美股分頁**：免金鑰（Yahoo 延遲約 15 分）。任意代碼（`AAPL`、`^GSPC`、`BRK-B`），預設 `^GSPC`+`VOO`，每 60 秒僅前景輪詢。休市時顯示前一交易日走勢。
* **資產歷史**：連線時每日自動記錄總資產（餘額+市值）收盤值；設定頁可手動補錄/匯出/匯入（匯入嚴格校驗整批通過才寫入，>3000 筆才清理保留最新 3000）。
* **TWSE 看板**：自選股重大訊息與除權息行事曆，每 10 分鐘更新，需對外網路。

## v1.6.0 新功能

* **API 憑證管理**（設定頁）：多組金鑰新增/編輯/切換/刪除。金鑰遮蔽顯示，編輯時留空或維持遮蔽＝不變更。切換/儲存啟用中設定需答 `PEA6`，觸發 shioaji 重啟（遮罩鎖定最長 30 秒，逾時可強制解鎖）。不可刪除最後一組或啟用中設定檔。金鑰填錯不會鎖死——設定頁永遠可用，修正後重新切換即可。
* **Demo 演示模式**（設定頁開關）：開啟後頁面重載，全部數據改為前端假數據（零實體請求、可完全離線）：餘額 1,250,300、四檔持倉、行情隨機漫步波動、可模擬下單成交（買進扣餘額加庫存、賣出限假庫存內）。Header 顯示 DEMO 徽章。關閉需答 `PEA6`。Demo 中不寫入真實資產歷史。

## 排障
* 帳務卡片顯示 `--`：永豐後台偶發緩慢，僅該卡片受影響。懷疑後台異常時關閉儀表板執行 `python sdk_probe.py` 區分問題來源。
* TWSE 憑證鏈缺 SKI：自動降級寬鬆 SSL（黃色提示，僅公開資料無風險）。
